import { Button as RadixButton } from "@radix-ui/themes"


const Button = () => {
    return (
        <RadixButton radius="full" variant="soft">
            Edit profile
        </RadixButton>
    )
}

export default Button